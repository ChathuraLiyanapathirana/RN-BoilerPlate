import React from 'react';
import { StyleSheet } from 'react-native';

const CustomStyles = StyleSheet.create({
    layoutStyle: {
        flex: 1,
        padding: 5
    },
    gridView: {
        marginTop: 10,
        flex: 1,
    },
    moviePosterImage:{
        width: '100%',
        height:200
    }
});

export { CustomStyles }